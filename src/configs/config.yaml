defaults:
  - config_schema
  - process_data/dataset_reader_manager: ghc_jigsaw_twitter

  - override hydra/job_logging: custom
  - override hydra/hydra_logging: disabled

  - _self_

hydra:
  output_subdir: null
  run: 
    dir: .

process_data:
  version: v3